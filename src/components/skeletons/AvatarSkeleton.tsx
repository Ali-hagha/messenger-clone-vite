import { Skeleton } from '../ui/skeleton';

const AvatarSkeleton = () => {
  return (
    <Skeleton className="rounded-full bg-gray-200 h-10 w-10 ring-2 ring-offset-2 ring-gray-200" />
  );
};

export default AvatarSkeleton;
